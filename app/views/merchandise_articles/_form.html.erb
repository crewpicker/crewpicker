<%= form_for(@merchandise_article) do |f| %>
  <% if @merchandise_article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@merchandise_article.errors.count, "error") %> prohibited this merchandise_article from being saved:</h2>

      <ul>
      <% @merchandise_article.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
