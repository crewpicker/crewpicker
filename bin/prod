#!/usr/bin/env bash

export RAILS_ENV=production

echo "Checking for pending migrations..."
if bundle exec rails db:migrate:status | grep -q "^\s*down"
then
  echo "ERROR: There are unapplied database migrations. Please apply migrations before starting."
  exit 1
else
  echo "No pending migrations."
fi

if ! command -v foreman &> /dev/null
then
  echo "Installing foreman..."
  gem install foreman
fi

foreman start -f Procfile.prod "$@"
